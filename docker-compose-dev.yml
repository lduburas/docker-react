services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    stdin_open: true
    tty: true
    ports:
      - 3000:5173
    command: npm run dev -- --host 0.0.0.0
    volumes:
      - .:/app
      - /app/node_modules